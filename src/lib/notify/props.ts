import { ToastContainerProps } from 'react-toastify';

export const toastifyProps: ToastContainerProps = {
  position: 'top-right',
  autoClose: 5000,
  hideProgressBar: true,
  pauseOnHover: true,
  theme: 'colored',
};
